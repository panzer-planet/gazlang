if ($a) { echo 1; }
if ($a) { echo 1; } else { echo 2; }
if ($a) { echo 1; } else if ($b) { echo 2; }
if ($a) {
    echo 1;
} else if ($b) {
    echo 2;
} else if ($c) {
    echo 3;
} else {
    echo 4;
}
if ($a = len($b)) { if ($c) { } else { } }
while ($i < 10) { $i++; }
while (true) { break; }
while ($a) { while ($b) { continue; } break; }
for ($i = 0; $i < 3; $i++) { echo $i; }
for ($i = 0;
    $i < 3;
    $i++) {
    for ($j = $i; $j > 0; --$j) { if ($j == 2) { break; } }
}
foreach ($xs as $x) { echo $x; }
foreach ($xs as $k => $v) { echo $k .. $v; }
foreach ($xs as @k => @v) { continue; }
foreach ([1, 2] as $x) { break; }
foreach (keys($m) as $i => [$a, $b]) { echo $a; }
foreach ($rows as [$name, $age,]) { echo $name; }
foreach ($rows as [$only]) { foreach ($only as $o) { break; } continue; }
delete $a[0];
delete $a["k"][1];
delete $rows[0]["total"];
delete $o.items[$i];
delete @g[0];
// break and continue belong to the innermost loop, also through if and match
while ($a) { if ($b) { match ($c) { 1 => { break; } default => { continue; } } } }

1 2
[1, 2, "hidden"]
2 2
Box.shut is not pub, so only the kind that declares it can use it
Box.hidden is not pub, so only the kind that declares it can use it
Box has no member nope
Box.shut is not pub, so only the kind that declares it can use it
{"open" => 1, "shut" => 2}
Box {#open => 1, #shut => 2}
